import { AppRegistry } from 'react-native';
import App from '../App';
import './App.css';

AppRegistry.registerComponent('BanjoHootson', () => App);

AppRegistry.runApplication('BanjoHootson', {
  rootTag: document.getElementById('root'),
});
